<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--全屏主题 Splash-->
    <style name="SplashFullscreen" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!--沉浸式主题 图片会延伸到状态栏-->
    <style name="TranslucentTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <!--Android 5.x开始需要把颜色设置透明，否则导航栏会呈现系统默认的浅灰色-->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!--BackableActivity-->
    <style name="BackableActivity" parent="Theme.AppCompat.Light">

    </style>

    <!-- 背景透明的dialog主题 -->
    <style name="TransparentDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- 背景透明的dialog主题 同时去掉自带的标题 -->
    <style name="TransparentNoTitleDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- 背景透明的dialog主题 同时去掉自带的标题
        弹出时自动弹出软键盘 -->
    <style name="Theme_comment_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <!--显示软键盘-->
        <item name="android:windowSoftInputMode">stateAlwaysVisible</item>
    </style>

    <!--自定义Dialog 弹出方式-->
    <style name="pop_anim_style">
        <item name="android:windowEnterAnimation">@anim/pop_in</item>
        <item name="android:windowExitAnimation">@anim/pop_out</item>
    </style>

</resources>